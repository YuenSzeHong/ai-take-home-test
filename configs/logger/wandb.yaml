wandb:
  _target_: pytorch_lightning.loggers.WandbLogger
  project: Tests
  name: ${now:%Y%m%d-%H%M%S}-${hydra:job.override_dirname}
  save_dir: logs/
  settings:
    _target_: wandb.Settings
    finish_timeout: 60
